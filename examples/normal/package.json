{
  "name": "normal-example",
  "scripts": {
    "init": "cnpm install",
    "clean": "rimraf ./build",
    "build": "npm run init && npm run clean && node ../../bin/miaow.js",
    "start": "npm run init && npm run clean && node ../../bin/miaow.js -w",
    "release": "npm run init && npm run clean && node ../../bin/miaow.js -p && npm run release.mv",
    "release.mv": "rimraf ./release && npm run release.mv.html && npm run release.mv.ftl",
    "release.mv.html": "cpy '**/*.*' '!**/*.ftl' '../release/html' --parents --cwd=build",
    "release.mv.ftl": "cpy '**/*.ftl' '../release/FE' --parents --cwd=build"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "iOS >= 8",
    "Android >= 4",
    "Explorer >= 8",
    "Firefox >= 43",
    "Chrome >= 45"
  ],
  "devDependencies": {
    "cpy-cli": "^1.0.1",
    "rimraf": "^2.6.1"
  }
}
