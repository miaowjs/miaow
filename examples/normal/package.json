{
  "name": "normal-example",
  "scripts": {
    "init": "cnpm install",
    "clean": "rimraf ./output",
    "build": "npm run init && npm run clean && node ../../bin/miaow.js",
    "release": "npm run init && npm run clean && node ../../bin/miaow.js -p"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "iOS >= 8",
    "Android >= 4",
    "Explorer >= 8",
    "Firefox >= 43",
    "Chrome >= 45"
  ],
  "devDependencies": {
    "rimraf": "^2.6.1"
  }
}
